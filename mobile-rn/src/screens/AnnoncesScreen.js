import ProductListScreen from "./ProductListScreen";

export default function AnnoncesScreen() {
  return <ProductListScreen mode="all" emptyText="Aucun produit pour l'instant — soyez le premier à en publier un." />;
}

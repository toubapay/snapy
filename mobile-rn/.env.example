# Optional override — defaults to http://10.0.2.2:4000 on Android emulator
# and http://localhost:4000 everywhere else (iOS simulator, web). Physical
# devices need your machine's LAN IP (printed when the backend starts).
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000
